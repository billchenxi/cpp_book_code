// Draft alias: the chapter narrative refers to the first CUDA example as
// "some-CUDA.cu" in one place and "add.cu" in another. This file keeps the
// draft naming usable by including the canonical example.
#include "add.cu"
